# AelitaOS Local Manifests Placeholder

This directory is the future local manifest location for AelitaOS source checkout configuration.

No real manifest exists yet.

Future manifests must:

- reference verified source trees only;
- avoid unknown or untrusted device repositories;
- avoid mixing non-`lisa` device trees into the `lisa` workspace;
- remain reviewable before any build claim.
